version: '3'
services:
  {{ service_name }}:
    image: "redis:alpine"
    container_name: {{service_name}}
    ports:
      - {{ redis_port }}:6379
    volumes:
      - {{ redis_conf_dir }}:/usr/local/etc/redis/redis.conf
      - {{ rdb_dir }}:/opt/rdb

